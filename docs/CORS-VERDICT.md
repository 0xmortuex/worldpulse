# CORS and reachability verdicts

Generated by `npm run probe` at 2026-08-12T14:13:09.840Z
Probed as `Origin: https://worldpulse.pages.dev`

Verdicts: **CLIENT-FETCH** the browser can read it directly · **WORKER-REQUIRED** it
must go through the edge proxy · **KEY-GATED** needs a key first · **UNREACHABLE**
network or policy failure.

Summary: 16 CLIENT-FETCH · 4 INCONCLUSIVE · 4 WORKER-REQUIRED · 3 UNREACHABLE · 6 KEY-GATED

| Source | Panel | HTTP | Access-Control-Allow-Origin | Verdict | Latency |
| --- | --- | --- | --- | --- | --- |
| `worldbank` | economy | 200 | `*` | **CLIENT-FETCH** | 436ms |
| `worldbank-milex` | military | 200 | `*` | **CLIENT-FETCH** | 76ms |
| `usgs-quakes` | layers | 200 | `*` | **CLIENT-FETCH** | 818ms |
| `nasa-eonet` | layers | 200 | `*` | **CLIENT-FETCH** | 1096ms |
| `wikidata-sparql` | government | 429 | `—` | **INCONCLUSIVE** | 543ms |
| `wikipedia-rest` | government | 200 | `*` | **CLIENT-FETCH** | 661ms |
| `wikimedia-commons` | header | 200 | `—` | **WORKER-REQUIRED** | 552ms |
| `gdelt-doc` | news | ERR | `—` | **UNREACHABLE** | 10487ms |
| `ucdp-ged` | risk | 401 | `—` | **INCONCLUSIVE** | 159ms |
| `ucdp-candidate` | risk | 401 | `—` | **INCONCLUSIVE** | 1172ms |
| `iptv-org-channels` | tv | 200 | `*` | **CLIENT-FETCH** | 269ms |
| `iptv-org-streams` | tv | 200 | `*` | **CLIENT-FETCH** | 245ms |
| `frankfurter` | economy | 200 | `*` | **CLIENT-FETCH** | 1372ms |
| `exchangerate-host` | economy | 200 | `*` | **KEY-GATED** | 654ms |
| `comtrade` | economy | 200 | `—` | **KEY-GATED** | 1545ms |
| `congress-gov` | legislature | 403 | `*` | **KEY-GATED** | 956ms |
| `uk-commons-votes` | legislature | 200 | `*` | **CLIENT-FETCH** | 2506ms |
| `howtheyvote-data` | legislature | 403 | `—` | **INCONCLUSIVE** | 324ms |
| `openparliament-ca` | legislature | 200 | `*` | **WORKER-REQUIRED** | 696ms |
| `camara-br` | legislature | 200 | `*` | **CLIENT-FETCH** | 11477ms |
| `abgeordnetenwatch` | legislature | 200 | `*` | **CLIENT-FETCH** | 1031ms |
| `nosdeputes` | legislature | 200 | `*` | **CLIENT-FETCH** | 964ms |
| `tweedekamer` | legislature | 200 | `*` | **CLIENT-FETCH** | 1072ms |
| `riksdagen` | legislature | 200 | `https://worldpulse.pages.dev` | **CLIENT-FETCH** | 1832ms |
| `theyvoteforyou` | legislature | 403 | `—` | **KEY-GATED** | 369ms |
| `epanstwo-pl` | legislature | ERR | `—` | **UNREACHABLE** | 301ms |
| `ofac-sdn` | risk | 200 | `—` | **WORKER-REQUIRED** | 2114ms |
| `eu-sanctions` | risk | 200 | `*` | **CLIENT-FETCH** | 856ms |
| `opensanctions` | risk | 200 | `https://worldpulse.pages.dev` | **KEY-GATED** | 759ms |
| `state-dept-advisories` | risk | 200 | `—` | **WORKER-REQUIRED** | 795ms |
| `fcdo-advisories` | risk | 200 | `*` | **CLIENT-FETCH** | 582ms |
| `smartraveller` | risk | ERR | `—` | **UNREACHABLE** | 20009ms |
| `eia` | economy | 403 | `*` | **KEY-GATED** | 712ms |

## Reasoning per source

- `worldbank` — **CLIENT-FETCH**. ACAO: *
- `worldbank-milex` — **CLIENT-FETCH**. ACAO: *
- `usgs-quakes` — **CLIENT-FETCH**. ACAO: *
- `nasa-eonet` — **CLIENT-FETCH**. ACAO: *
- `wikidata-sparql` — **INCONCLUSIVE**. Upstream returned HTTP 429; no usable ACAO on the error response. CORS posture on the success path is unknown — re-probe with a request that succeeds.
- `wikipedia-rest` — **CLIENT-FETCH**. ACAO: *
- `wikimedia-commons` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `gdelt-doc` — **UNREACHABLE**. Connect Timeout Error (attempted address: api.gdeltproject.org:443, timeout: 10000ms)
- `ucdp-ged` — **INCONCLUSIVE**. Upstream returned HTTP 401; no usable ACAO on the error response. CORS posture on the success path is unknown — re-probe with a request that succeeds.
- `ucdp-candidate` — **INCONCLUSIVE**. Upstream returned HTTP 401; no usable ACAO on the error response. CORS posture on the success path is unknown — re-probe with a request that succeeds.
- `iptv-org-channels` — **CLIENT-FETCH**. ACAO: *
- `iptv-org-streams` — **CLIENT-FETCH**. ACAO: *
- `frankfurter` — **CLIENT-FETCH**. ACAO: *
- `exchangerate-host` — **KEY-GATED**. Public key VITE_EXCHANGERATE_HOST_KEY; ACAO observed: *.
- `comtrade` — **KEY-GATED**. Key COMTRADE_KEY is server-side; routed through the Worker regardless of ACAO (observed: none).
- `congress-gov` — **KEY-GATED**. Key CONGRESS_GOV_KEY is server-side; routed through the Worker regardless of ACAO (observed: *).
- `uk-commons-votes` — **CLIENT-FETCH**. ACAO: *
- `howtheyvote-data` — **INCONCLUSIVE**. Upstream returned HTTP 403; no usable ACAO on the error response. CORS posture on the success path is unknown — re-probe with a request that succeeds.
- `openparliament-ca` — **WORKER-REQUIRED**. Requires a descriptive User-Agent, which browsers are forbidden to set. ACAO observed: *.
- `camara-br` — **CLIENT-FETCH**. ACAO: *
- `abgeordnetenwatch` — **CLIENT-FETCH**. ACAO: *
- `nosdeputes` — **CLIENT-FETCH**. ACAO: *
- `tweedekamer` — **CLIENT-FETCH**. ACAO: *
- `riksdagen` — **CLIENT-FETCH**. ACAO: https://worldpulse.pages.dev
- `theyvoteforyou` — **KEY-GATED**. Key THEYVOTEFORYOU_KEY is server-side; routed through the Worker regardless of ACAO (observed: none).
- `epanstwo-pl` — **UNREACHABLE**. Request was cancelled.
- `ofac-sdn` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `eu-sanctions` — **CLIENT-FETCH**. ACAO: *
- `opensanctions` — **KEY-GATED**. Key OPENSANCTIONS_KEY is server-side; routed through the Worker regardless of ACAO (observed: https://worldpulse.pages.dev).
- `state-dept-advisories` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `fcdo-advisories` — **CLIENT-FETCH**. ACAO: *
- `smartraveller` — **UNREACHABLE**. The operation was aborted due to timeout
- `eia` — **KEY-GATED**. Key EIA_KEY is server-side; routed through the Worker regardless of ACAO (observed: *).

## Not probed

- `naturalearth` — no runtime fetch. Bundled from npm at build time. No runtime fetch.
- `globalfirepower` — excluded. Per decision 2: excluded. No API, no licence grant, proprietary compiled estimates. Named in the Military tab's no-data card as a reason equipment inventories are unavailable.
- `iiss-military-balance` — excluded. Per decision 2: excluded. Named in the Military tab's no-data card.
- `acled` — excluded. Excluded per Phase 0 section 1.1. UCDP GED is the conflict source.
- `sipri-direct` — excluded. No API. Reached indirectly via worldbank-milex. Link-out only. Arms-transfer arcs dropped per decision 4.
- `un-register-arms` — excluded. Excluded per decision 4. Self-reported and sparse; drawn as arcs, a missing report reads as 'no transfers', which is a confident falsehood. Replaced by a coverage note in the Military tab.
