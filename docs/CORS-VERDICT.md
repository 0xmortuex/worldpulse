# CORS and reachability verdicts

Generated by `npm run probe` at 2026-08-12T15:29:42.187Z
Probed as `Origin: https://worldpulse.pages.dev`

Verdicts: **CLIENT-FETCH** the browser can read it directly · **WORKER-REQUIRED** it
must go through the edge proxy · **KEY-GATED** needs a key first · **UNREACHABLE**
network or policy failure.

Summary: 19 CLIENT-FETCH · 2 UNREACHABLE · 4 WORKER-REQUIRED · 6 KEY-GATED · 1 INCONCLUSIVE

| Source | Panel | HTTP | Access-Control-Allow-Origin | Verdict | Latency | Probed |
| --- | --- | --- | --- | --- | --- | --- |
| `worldbank` | economy | 200 | `*` | **CLIENT-FETCH** | 436ms | unknown |
| `worldbank-milex` | military | 200 | `*` | **CLIENT-FETCH** | 76ms | unknown |
| `usgs-quakes` | layers | 200 | `*` | **CLIENT-FETCH** | 818ms | unknown |
| `nasa-eonet` | layers | 200 | `*` | **CLIENT-FETCH** | 1096ms | unknown |
| `wikidata-sparql` | government | 200 | `*` | **CLIENT-FETCH** | 830ms | 2026-08-12 15:16 |
| `wikipedia-rest` | government | 200 | `*` | **CLIENT-FETCH** | 661ms | unknown |
| `wikimedia-commons` | header | 200 | `*` | **CLIENT-FETCH** | 609ms | **this run** |
| `gdelt-doc` | news | ERR | `—` | **UNREACHABLE** | 11119ms | 2026-08-12 15:17 |
| `ucdp-ged` | risk | 200 | `—` | **WORKER-REQUIRED** | 1225ms | 2026-08-12 15:19 |
| `ucdp-candidate` | risk | 200 | `—` | **WORKER-REQUIRED** | 1631ms | 2026-08-12 15:20 |
| `iptv-org-channels` | tv | 200 | `*` | **CLIENT-FETCH** | 269ms | unknown |
| `iptv-org-streams` | tv | 200 | `*` | **CLIENT-FETCH** | 245ms | unknown |
| `frankfurter` | economy | 200 | `*` | **CLIENT-FETCH** | 1372ms | unknown |
| `exchangerate-host` | economy | 200 | `*` | **KEY-GATED** | 654ms | unknown |
| `comtrade` | economy | 200 | `—` | **KEY-GATED** | 1545ms | unknown |
| `congress-gov` | legislature | 403 | `*` | **KEY-GATED** | 956ms | unknown |
| `uk-commons-votes` | legislature | 200 | `*` | **CLIENT-FETCH** | 2506ms | unknown |
| `howtheyvote-data` | legislature | 403 | `—` | **INCONCLUSIVE** | 346ms | 2026-08-12 15:18 |
| `openparliament-ca` | legislature | 200 | `*` | **CLIENT-FETCH** | 668ms | 2026-08-12 15:16 |
| `camara-br` | legislature | 200 | `*` | **CLIENT-FETCH** | 11477ms | unknown |
| `abgeordnetenwatch` | legislature | 200 | `*` | **CLIENT-FETCH** | 1031ms | unknown |
| `nosdeputes` | legislature | 200 | `*` | **CLIENT-FETCH** | 964ms | unknown |
| `tweedekamer` | legislature | 200 | `*` | **CLIENT-FETCH** | 1072ms | unknown |
| `riksdagen` | legislature | 200 | `https://worldpulse.pages.dev` | **CLIENT-FETCH** | 1832ms | unknown |
| `theyvoteforyou` | legislature | 403 | `—` | **KEY-GATED** | 369ms | unknown |
| `ofac-sdn` | risk | 200 | `—` | **WORKER-REQUIRED** | 2114ms | unknown |
| `eu-sanctions` | risk | 200 | `*` | **CLIENT-FETCH** | 856ms | unknown |
| `opensanctions` | risk | 200 | `https://worldpulse.pages.dev` | **KEY-GATED** | 759ms | unknown |
| `state-dept-advisories` | risk | 200 | `—` | **WORKER-REQUIRED** | 795ms | unknown |
| `fcdo-advisories` | risk | 200 | `*` | **CLIENT-FETCH** | 582ms | unknown |
| `smartraveller` | risk | ERR | `—` | **UNREACHABLE** | 20007ms | 2026-08-12 15:23 |
| `eia` | economy | 403 | `*` | **KEY-GATED** | 712ms | unknown |

## Reasoning per source

- `worldbank` — **CLIENT-FETCH**. ACAO: *
- `worldbank-milex` — **CLIENT-FETCH**. ACAO: *
- `usgs-quakes` — **CLIENT-FETCH**. ACAO: *
- `nasa-eonet` — **CLIENT-FETCH**. ACAO: *
- `wikidata-sparql` — **CLIENT-FETCH**. ACAO: *
- `wikipedia-rest` — **CLIENT-FETCH**. ACAO: *
- `wikimedia-commons` — **CLIENT-FETCH**. ACAO: *
- `gdelt-doc` — **UNREACHABLE**. Connect Timeout Error (attempted address: api.gdeltproject.org:443, timeout: 10000ms)
- `ucdp-ged` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `ucdp-candidate` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `iptv-org-channels` — **CLIENT-FETCH**. ACAO: *
- `iptv-org-streams` — **CLIENT-FETCH**. ACAO: *
- `frankfurter` — **CLIENT-FETCH**. ACAO: *
- `exchangerate-host` — **KEY-GATED**. Public key VITE_EXCHANGERATE_HOST_KEY; ACAO observed: *.
- `comtrade` — **KEY-GATED**. Key COMTRADE_KEY is server-side; routed through the Worker regardless of ACAO (observed: none).
- `congress-gov` — **KEY-GATED**. Key CONGRESS_GOV_KEY is server-side; routed through the Worker regardless of ACAO (observed: *).
- `uk-commons-votes` — **CLIENT-FETCH**. ACAO: *
- `howtheyvote-data` — **INCONCLUSIVE**. Upstream returned HTTP 403; an error response is not evidence about the success path (ACAO observed on it: none). Re-probe with a request that succeeds.
- `openparliament-ca` — **CLIENT-FETCH**. ACAO: *
- `camara-br` — **CLIENT-FETCH**. ACAO: *
- `abgeordnetenwatch` — **CLIENT-FETCH**. ACAO: *
- `nosdeputes` — **CLIENT-FETCH**. ACAO: *
- `tweedekamer` — **CLIENT-FETCH**. ACAO: *
- `riksdagen` — **CLIENT-FETCH**. ACAO: https://worldpulse.pages.dev
- `theyvoteforyou` — **KEY-GATED**. Key THEYVOTEFORYOU_KEY is server-side; routed through the Worker regardless of ACAO (observed: none).
- `ofac-sdn` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `eu-sanctions` — **CLIENT-FETCH**. ACAO: *
- `opensanctions` — **KEY-GATED**. Key OPENSANCTIONS_KEY is server-side; routed through the Worker regardless of ACAO (observed: https://worldpulse.pages.dev).
- `state-dept-advisories` — **WORKER-REQUIRED**. No Access-Control-Allow-Origin on the response.
- `fcdo-advisories` — **CLIENT-FETCH**. ACAO: *
- `smartraveller` — **UNREACHABLE**. The operation was aborted due to timeout
- `eia` — **KEY-GATED**. Key EIA_KEY is server-side; routed through the Worker regardless of ACAO (observed: *).

## Not probed

- `naturalearth` — no runtime fetch. Bundled from npm at build time. No runtime fetch.
- `globalfirepower` — excluded. Per decision 2: excluded. No API, no licence grant, proprietary compiled estimates. Named in the Military tab's no-data card as a reason equipment inventories are unavailable.
- `iiss-military-balance` — excluded. Per decision 2: excluded. Named in the Military tab's no-data card.
- `acled` — excluded. Excluded per Phase 0 section 1.1. UCDP GED is the conflict source.
- `sipri-direct` — excluded. No API. Reached indirectly via worldbank-milex. Link-out only. Arms-transfer arcs dropped per decision 4.
- `un-register-arms` — excluded. Excluded per decision 4. Self-reported and sparse; drawn as arcs, a missing report reads as 'no transfers', which is a confident falsehood. Replaced by a coverage note in the Military tab.
