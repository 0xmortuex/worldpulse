{
  "name": "worldpulse",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.tests.json",
    "probe": "NODE_USE_ENV_PROXY=1 node scripts/probe-sources.mjs",
    "test": "node scripts/run-tests.mjs",
    "attribution": "node scripts/gen-attribution.mjs",
    "verify": "npm run build && node scripts/verify-render.mjs",
    "check:deploy": "node scripts/check-deploy-gate.mjs",
    "mutate": "node scripts/mutation-check.mjs"
  },
  "dependencies": {
    "globe.gl": "^2.46.1",
    "i18n-iso-countries": "^7.14.0",
    "three": "^0.185.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/three": "^0.185.4",
    "@types/topojson-client": "^3.1.5",
    "playwright": "^1.62.1",
    "ts-analyzer": "npm:typescript@5.9.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
